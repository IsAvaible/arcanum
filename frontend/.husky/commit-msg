cd frontend

echo "Verifying commit message..."

# Verify that commit messages meet the Conventional Commits spec.
npx commitlint  --edit "$1"

echo "Commit message verified."

cd ..
# Make the pre-commit script executable
chmod +x ./frontend/.husky/pre-commit.sh
# Run the pre-commit checks
./frontend/.husky/pre-commit.sh